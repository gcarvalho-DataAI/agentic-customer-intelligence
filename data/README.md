# Data

Input dataset for the ML clustering module.

```text
dados_sinteticos.csv
```

The file is intentionally kept outside the ML module so later API, agent, or evaluation scripts can consume the same source data without duplicating it.

